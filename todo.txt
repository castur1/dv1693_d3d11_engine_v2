* Requirements:

    X Deferred rendering
    X Shadow mapping
    * Particle system <--
    X Dynamic cube mapping
    * Tessellation
    X Hierarchical view frustum culling
    X Normal mapping

* Bind enums

    * This can be semi-solved manually by circumventing BIND, like in Transform, but still only int in editor

    * Some way of setting a list of items would be useful as well, like for keyframes and animations

    * Also setting colours, displaying textures...

* Ambient Occlusion and other post-processing effects? Bloom?

* Continue working on the debug overlay / editor

    * Add/remove entities and components

    * Pause and frame-by-frame

    * isStatic toggle

    * Editor currently crashes if you change scene while an entity is selected

* XMVECTOR versions of Get/Set in Transform

* Make renderer debugMode a setting instead, maybe?

* Assets that are loaded by other assets (e.g. textures loaded by models) are not present in the asset section and 
  will thus be unloaded. Since the owning asset is still loaded, those child asset will never get reloaded.

    * Rethink asset reuse. Or just include the sub-assets in the asset list. 

* Improve culling

    * Freezing camera in place while moving the view frustum (for culling visualization)

    * Use an array instead of a vector in the octree node. Should be faster, right?

        * Problem with overlapping renderables?

    * When I add/remove components from an entity, am I correctly updating the octree? Look for other bugs like that.

        * No, I don't think I am. Component are only added to culling at load. Fix!

    * Minor problem with the current component culling approach is that only whole models are culled or not, rather than
      its sub-models. Culling the sub-models could potentially be faster but there's no good way of doing that if we're culling the
      entire component.

        * Solution! The ModelRenderer can do culling on its sub-models itself! That way we get the best of both worlds :D

* Improve shadows

    * Cascading shadow maps?

    * Oriented Bounding Box for orthographic view culling (directional light)

    * Static lights, so that we don't have to recompute all the matrices and stuff each frame?

* Better error handling

* Look everything over. Everything. Lots of things could probably be refactored. 

    * The way I submit lights and cube maps feels a bit weird. Renderer is also way too big, probably

* Make shadow sampler common, probably

* FixedUpdate()

* Animated particles?


For skyboxes:
bk -> pz
dn -> ny
ft -> nz
lf -> nx
rt -> px
up -> py
...and then a 90 degree rotation CCW