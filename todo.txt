* Requirements:

    X Deferred rendering
    * Shadow mapping
    * Particle system
    * Dynamic cube mapping
    * Tessellation
    ? Hierarchical view frustum culling
    X Normal mapping

* Refactor the Blinn-Phong implementation

* Bind enums

    * This can be semi-solved manually by circumventing BIND, like in Transform, but still only int in editor

    * Some way of setting a list of items would be useful as well, like for keyframes and animations

* Ambient Occlusion and other post-processing effects? Bloom?

* Continue working on the debug overlay / editor

    * Add/remove entities and components

    * Pause and frame-by-frame

    * Display camera position (and rotation)

    * isStatic

* XMVECTOR versions of Get/Set in Transform

* Minor problem with the current component culling approach is that only whole models are culled or not, rather than
its sub-models. Culling the sub-models could potentially be faster but there's no good way of doing that if we're culling the
entire component.

    * Solution! The ModelRenderer can do culling on its sub-models itself! That way we get the best of both worlds :D

* When I add/remove components from an entity, am I correctly updating the octree? Look for other bugs like that.

    * No, I don't think I am. Component are only added to culling at load. Fix!

* "Scene-global"? Why are we only submitting them to the primary view?

* Verify that the octree actually works. Debug lines or smth

    * Move camera without moving frustum (or the opposite), and show bounding box wireframes?

* Use an array instead of a vector in the octree node. Should be faster, right?

    * Problem with overlapping renderables. 

* Some sort of actual debug system as a core system in the engine would be nice.


I need some global way to read/write variables for debugging. Both for setting options in the editor to read elsewhere, and
to set data that I want to display in the editor. I'll probably do something similar to my current Input system where it's
directly globally accessible. How would I handle different data types? Do I want to keep "setting" (bools) and generic data seperate?
Probably keep it per-frame (for data?), not persisten. I also need some way of drawing things to the screen for visual debugging. 
Maybe I could add a render pass for that in my frame graph at the very end, seems like the easiest solution. Maybe keeping a global line list
as part of the debug system would suffice. "Debug" (for data) and "DebugDraw" (for drawing), or just one system? Maybe other names. 